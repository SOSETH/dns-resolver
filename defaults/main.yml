# Size of the local DNS cache in MB
dns_unbound_cache_size: 4096

# DNS search domain, used for /etc/resolv.conf
dns_search_domain: 'sos.ethz.ch'

# Localhost only?
dns_unbound_local_only: True

# Fallback resolver for bootstrap
dns_fallback_nameserver: 8.8.8.8
